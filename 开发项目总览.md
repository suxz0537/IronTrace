# 铁迹 IronTrace - 开发项目总览

> 供后续开发（含 Agent 协作）参考的核心文档：**项目介绍 / 已完成工作记录 / 后续待办清单**

---

## 1. 项目介绍

### 1.1 项目定位
- **名称**：铁迹 IronTrace
- **类型**：微信小程序
- **用途**：个人健身记录工具，用于记录每次训练的动作、重量、组数，后续提供统计分析和训练建议
- **当前范围**：V1（基础记录）+ V2（体验优化）+ V3（云同步+统计）；训练计划、高级建议属于 V4 暂不做

### 1.2 目标用户
- 第一阶段：开发者本人（个人使用，可不发布，仅开发版/体验版即可）
- 后续扩展：其他健身爱好者

### 1.3 核心目标
1. 快速记录每次训练数据（动作、重量、组数、次数）
2. 自动填充上次训练重量，减少输入成本
3. 数据云端同步，防止丢失
4. 展示训练容量、1RM 趋势和 PR 记录

### 1.4 技术栈（已锁定）
| 层级 | 选型 | 说明 |
|------|------|------|
| 前端语言 | **TypeScript** | 严格模式；类型安全，匹配复杂数据模型 |
| 小程序框架 | 微信原生小程序 | libVersion 3.0.0 |
| UI 组件库 | **Vant Weapp**（^1.11.7） | 步进器/表单组件适配健身录入，社区资源丰富 |
| 图表库 | **ECharts for 微信小程序** | 支持双轴、标记点，满足 V3 统计图表 |
| 后端/存储 | 微信云开发 | V1 纯本地 Storage，V3 迁移云数据库+云函数+云存储 |
| 状态传递 | `app.globalData` + Storage | 简单场景够用，无需引入 MobX/Redux |

### 1.5 项目目录结构
```
IronTrace/
├── 需求.md                               # 主需求文档（V1-V3 权威依据）
├── 开发项目总览.md                        # 本文件
├── package.json / package-lock.json      # 依赖：@vant/weapp、typescript、miniprogram-api-typings
├── project.config.json                   # 微信开发者工具项目配置
├── tsconfig.json                         # TypeScript 严格模式 + @ 路径别名
└── miniprogram/
    ├── app.json / .ts / .wxss            # TabBar 配置、全局组件注册、全局样式
    ├── sitemap.json
    ├── data/
    │   └── exercises.json                # 54 个内置动作（带动作说明）
    ├── types/
    │   ├── index.d.ts                    # 6 个数据模型 interface + 8 个枚举 type
    │   └── app.d.ts                      # AppOptions / Page 泛型
    ├── utils/
    │   ├── constants.ts                  # 4 组枚举中英文映射 + getLabel 函数
    │   ├── helpers.ts                    # 拼音首字母匹配 / 分组 / 格式化 / 排序
    │   └── storage.ts                    # CRUD + 容量/1RM 计算 + 边界处理
    └── pages/（7 个页面四件套：.ts/.wxml/.wxss/.json）
        ├── training/                     # 训练首页（TabBar）
        ├── training-detail/              # 训练录入详情（未完成 isCompleted=false 的会话）
        ├── exercise-list/                # 动作库（TabBar，支持 mode=pick 选动作）
        ├── exercise-detail/              # 动作详情 + 选动作跳转
        ├── exercise-add/                 # V1 自定义动作表单（仅添加）
        ├── history-list/                 # 历史训练列表（入口在训练首页）
        └── history-detail/               # 历史训练详情（只读）
```

---

## 2. 已完成工作记录

### 2.1 里程碑 M1：需求澄清与决策锁定（2026-09-03）
从原始需求初稿中识别了 30+ 处不明确点，与用户确认后全部写入文档：

| 决策点 | 最终结论 |
|--------|----------|
| 字段/枚举使用语言 | **数据库存英文，界面显示中文** |
| 本地存储 V1 结构 | **sessions 内嵌 sets**（读写简单，V3 迁移时拆分） |
| 技术选型 | TypeScript + Vant Weapp + ECharts |
| 自定义动作 | V1 就支持**添加**（编辑/删除留 V2） |
| 历史记录入口 | V1 训练首页顶部"上次训练"卡片 + "查看全部"按钮，V3 再进 TabBar |
| 训练异常中断 | 下次打开弹窗：继续上次训练 / 丢弃 |
| 新训练发现上次未结束 | **自动结束上次**，toast 提示"上次已自动保存" |
| 时长异常（>240min） | 详情页提示"时长异常，可手动编辑" |
| 重量/次数校验 | 重量：0~1000，步长 0.5；次数：1~100 整数 |
| 热身组过滤 | 容量 / 1RM / PR 一律只算 `isWarmup=false` 正式组 |
| Epley 公式边界 | reps=1 时 1RM 直接取重量 |
| 重量输入 UI | ±2.5kg / ±1.25kg 快捷按钮（匹配片重） |
| RPE 录入 | V1 做，1~10 整数步进器，非必填，文案提示"10=完全力竭" |
| 组间休息默认值 | V1 留空；V2 默认 120s 全局统一；V3 支持每动作自定义 |
| PR 定义 | 留到 V3 再细化 |

### 2.2 里程碑 M2：需求文档更新（2026-09-03）
对 `需求.md` 做了 11 处编辑修改（覆盖 3.1、3.2、4.1、4.2、4.3 新增、5.1.x×4、5.2.2、6.2、7、8、9 章节）。主要变化：

1. **3.1**：技术选型从"或"改为"锁定为具体技术"
2. **3.2**：TabBar 注释明确 V1 仅 2 Tab + 历史入口在训练首页
3. **4.1**：storage key 明确为 `builtinExercises/customExercises/sessions`，并附 sets 内嵌结构示例
4. **4.2**：exercises 模型补 `isCustom` 字段、id 前缀说明、枚举映射表指引
5. **4.3 新增章节**：bodyPart(8)/equipment(7)/type(2)/trainingGoal(4) 四组完整中英文映射表
6. **5.1.1 动作库**：补自定义动作表单规范 + 拼音首字母搜索说明
7. **5.1.2 自由训练记录**：补字段校验规则、备注功能、边界处理 4 条、热身组过滤原则
8. **5.1.3 历史记录**：补入口位置、卡片展示内容、删除级联说明
9. **5.1.4 验收标准**：细化为 4 条（含异常退出弹窗、拼音搜索、自定义动作持久化）
10. **5.2.2 组间计时器**：默认 120s 全局 + ±10s 交互 + 提醒方式
11. **6.2 / 7 / 8 / 9**：Epley reps=1 边界处理、里程碑文字、云权限规则、附录字段改为英文枚举

### 2.3 里程碑 M3：项目框架搭建（2026-09-03）
#### 2.3.1 配置文件
- `package.json`：声明 `@vant/weapp@^1.11.7`、`typescript@^5.4.0`、`miniprogram-api-typings@^3.12.0`
- `project.config.json`：appid 用 `touristappid`（体验版），`miniprogramRoot: "miniprogram/"`，nodeModules 启用
- `tsconfig.json`：strict 全开，`@/*` 指向 `miniprogram/*`
- `app.json`：2 个 TabBar（训练/动作库），全局注册 20 个 Vant 组件，7 个页面预注册
- `app.wxss`：全局配色（#1989fa 主题蓝）、卡片/标签/间距工具类

#### 2.3.2 类型系统（`types/`）
- `Exercise` / `WorkoutSet` / `WorkoutSession` / `UserInfo` / `BodyMetric` / `EnumOption` 6 个 interface
- `BodyPart` / `Equipment` / `ExerciseType` / `TrainingGoal` / `StorageKey` 等枚举联合类型
- 所有模型严格对齐 `需求.md` 4.1/4.2，避免需求与实现偏差

#### 2.3.3 数据与工具（`data/` + `utils/`）
- `data/exercises.json`：**54 个内置动作**，按胸(10)/背(11)/腿(12)/肩(6)/臂(7)/核心(5)/有氧(2)/全身(1) 分布，全附带中文动作说明
- `utils/constants.ts`：4 组枚举中英文映射数组 + `getBodyPartLabel()` 等 4 个便捷查询函数
- `utils/helpers.ts`：拼音首字母匹配（50+ 常用字首字母表）、分组函数、日期/时长/容量格式化、部位排序常量
- `utils/storage.ts`（**核心 23 个函数**）：
  - 工具：`safeGetStorageSync` / `safeSetStorageSync` / `genId`
  - 动作库：`initExercises` / `getBuiltinExercises` / `getCustomExercises` / `getAllExercises` / `saveCustomExercise`
  - 会话 CRUD：`getSessions` / `getSessionById` / `saveSession` / `deleteSession`
  - 训练流程：`getPendingSession` / `createNewSession`（自动结束上次）/ `finishSession`
  - 动作+组：`addExerciseToSession` / `updateSet` / `removeSet`
  - 算法：`calcSessionVolume`（热身组过滤）/ `calcSessionDurationMin` / `calc1RM`（reps=1 特殊处理）

#### 2.3.4 7 个页面骨架
| 页面 | 数据链路状态 | 主要 UI 组件 |
|------|:----------:|-------------|
| **training** 训练首页 | ✅ 已连通 | 上次训练卡片（van-card）+ 未完成弹窗（wx.showModal）+ 开始训练按钮（van-button）+ 空状态 |
| **exercise-list** 动作库 | ✅ 已连通 + **mode=pick** 适配 | van-search（拼音+中文搜索）+ 部位筛选横向 tag + 部位分组 van-cell-group + 右上 "+" 添加按钮 |
| **exercise-add** 自定义动作 | ✅ 已连通 | 4 字段表单（van-field + 3 个 van-picker 弹窗）+ 校验 + saveCustomExercise |
| **exercise-detail** 动作详情 | ✅ 已连通 + pick 模式 | 元信息展示（van-cell-group）+ pick 模式底部"添加到本次训练"按钮 |
| **training-detail** 训练录入 | ✅ 已连通 | 顶部摘要卡 + 按动作分组 + 每组 stepper/switch 实时 updateSet 保存 + 右滑删组 + submit-bar 备注 + 结束训练按钮 |
| **history-list** 历史列表 | ✅ 已连通 | 右滑 van-swipe-cell 删除（二次确认）+ 卡片展示日期/时长/容量/动作数 |
| **history-detail** 历史详情 | ✅ 已连通 | 同 training-detail 布局但所有 stepper/switch `disabled` 只读 |

**开发验证**：VSCode `GetDiagnostics` 返回空数组，全项目无类型/语法错误。

---

## 3. 后续待办清单（按版本阶段划分）

### 3.1 V1 阶段待办（基础记录功能 - 验收前必做）

#### 📋 P0 - 高优（不做则 V1 验收不通过）
| # | 任务 | 关联需求章节 | 关键实现点 |
|---|------|:----------:|-----------|
| V1-01 | 微信开发者工具中 **构建 npm**，验证 Vant 组件引入 | 技术架构 3.1 | `miniprogram_npm/@vant/weapp/**` 必须生成，否则 van-* 组件白屏 |
| V1-02 | TabBar **图标资源补齐** | `app.json` tabBar.iconPath | 准备 4 张 PNG（训练/动作库 × 普通/选中），放入 `assets/tabbar/` |
| V1-03 | **集成自测一次完整链路**：训练首页 → 开始训练 → +动作 → 录 3 组 → 结束 → 查看历史详情 → 删除 → 确认 Storage 同步 | 需求 5.1.4 | 重点测：① sets 内嵌写入/读出正确性 ② 容量只累计正式组 ③ 杀进程再开弹窗恢复 |
| V1-04 | **拼音首字母表扩充**（utils/helpers.ts `PinyinFirstLetterMap`） | 需求 5.1.1 | 当前只有 ~50 字，需覆盖 54 个内置动作的全部字 + 常见自定义动作用字，否则"wt 匹配卧推"会漏 |
| V1-05 | 训练首页：**添加自定义动作后，在动作库里立刻能看到**（刷新） | 需求 5.1.1 | 目前 exercise-add 已执行 `app.globalData.exercises = getAllExercises()`，需 exercise-list 的 onShow 实际拉 globalData 或重查 Storage 验证 |
| V1-06 | 训练录入：**stepper 快捷 ±2.5kg / ±1.25kg 按钮 UI** | 需求 8. 注意事项 | 现在 stepper 步长 0.5，需求额外有两个快捷按钮；在每组 stepper 旁加两个小按钮或自定义步进步进方式 |
| V1-07 | exercise-list mode=pick：从 training-detail 跳转时**回传 sessionId** | 需求 5.1.2 | 目前用 getPendingSession 兜底，但如果有多个未完成会话（异常情况）需准确关联 |

#### 🟡 P1 - 中优（影响体验，但有 workaround）
| # | 任务 | 说明 |
|---|------|------|
| V1-08 | RPE 文案：stepper 显示 1~10 之外加一个"关闭/清空"，因为目前 1 是最低但用户想留空 | 现在 rpe 最小值 1，无法在 stepper 上表达"未填"，需要额外开关 |
| V1-09 | 训练录组：**每组支持新增、删除，但缺"复制上一组"快捷操作** | 大多数用户连续组的重量/次数相同，一键复制能省大量输入 |
| V1-10 | 训练录组：训练中途 **在动作之间切换位置**（拖动排序） | sets 内嵌数组顺序决定展示顺序，目前新增只能追加到末尾 |
| V1-11 | 训练录组：**未完成会话，再次进入时需要 scroll 到当前录入位置** | 训练动作多的时候，每次打开页面都从顶部开始体验差 |
| V1-12 | 历史详情：**时长异常 4h+ 时，提供手动修正按钮**，目前只是文字提示不友好 | |

#### 🔵 P2 - 低优（锦上添花）
| # | 任务 | 说明 |
|---|------|------|
| V1-13 | 动作库：支持按器械筛选（与部位筛选并列，二级筛选） | 目前只支持部位筛选 |
| V1-14 | 动作库：列表支持「分组/平铺」模式切换 | 两种视图用户各有偏好 |
| V1-15 | 训练首页：顶部展示"本周训练天数"小提示 | 简单从 sessions 聚合即可 |
| V1-16 | 自定义动作：新增时实时预览与内置动作并排比较 | |

### 3.2 V2 阶段待办（体验优化 - 依赖 V1 完成）
| # | 任务 | 关联需求 | 关键点 |
|---|------|:------:|--------|
| V2-01 | **自动填充上次重量/次数**：选动作后 → 查询最近一次同动作正式组 → auto fill | 5.2.1 | 核心函数在 storage.ts 中新增 `getLastSetsOfExercise(exerciseId)` |
| V2-02 | **组间计时器**：每组录完后自动开始倒计时，默认 120s，支持暂停/跳过/±10s/结束震动提醒 | 5.2.2 | 小程序后台运行需要检查倒计时是否准确，必要时用 worker |
| V2-03 | **历史编辑**：历史详情页由"只读"改为"编辑模式切换"，支持改重量/次数/删组/插组/改开始结束时间和备注 | 5.2.3 | 目前 stepper 只要去掉 disabled + 复用 updateSet 即可，工作量中等 |
| V2-04 | **自定义动作编辑+删除**（V1 只做了新增） | 需求决定项 | V1 中 exercise-add 改造成复用表单，新增 edit 模式 |
| V2-05 | 历史列表：提供**按部位/动作筛选**，当前只能时间倒序浏览 | 5.2.3 | 纯前端过滤，不涉及数据层 |
| V2-06 | 连续录组手感：用 **震动反馈 + toast** 标记一组完成 | 细节 | wx.vibrateShort |

### 3.3 V3 阶段待办（统计 + 云同步 - 前置依赖需求细化）
**前置提醒：以下项目需要先细化需求才能动手**

| # | 任务 | 前置需求（需先拍板） |
|---|------|-------------------|
| V3-01 | 开通微信云开发环境，创建 5 个集合 + 配置权限规则 | 集合命名、初始环境 ID |
| V3-02 | **V1→V3 数据迁移工具**：内嵌 sets 拆为 workout_sessions + workout_sets 两张表（关键：setNo 拆分后 exerciseId 属于 session 下哪个动作段，目前 model 缺 workout_session_exercises 关联表——需要先补模型设计） | 需求 4 数据模型补表、迁移策略是自动还是手动按钮 |
| V3-03 | **PR 定义细化**：最大重量？最大 1RM？最大单组容量？每动作同时存多个 PR？ | 需求 5.3.3 |
| V3-04 | 训练容量趋势图、1RM 趋势图（ECharts）：近 30/90 天、双轴叠加、PR 标记点 | 图表交互细节、无训练日显示 0 还是断开 |
| V3-05 | 周训练频率柱状图 | 按自然周还是滚动窗口 |
| V3-06 | 离线同步 + 冲突解决策略 | 需求 5.3.2 |
| V3-07 | body_metrics 录入页（我的 Tab） | 需求 4.2 定义了模型，但页面功能需求未写 |
| V3-08 | 数据导出为 JSON/CSV：小程序无法直接下载 → 分享文件到聊天 / 复制剪贴板 / 发送邮件 | 需求 5.3.4 |
| V3-09 | 扩展 TabBar：从 2 Tab → 4 Tab（训练/动作库/统计/我的） | Tab 图标设计 |

### 3.4 跨阶段基础工作（不区分版本，随时可做）
| # | 任务 | 说明 |
|---|------|------|
| X-01 | **单元测试**：为 utils/storage.ts 23 个函数写单测（容量/1RM 算法边界用例必须齐全） | |
| X-02 | **ESLint + Prettier** 接入，统一团队/Agent 代码风格 | package.json scripts 目前是空 |
| X-03 | 组件抽取：训练录组中的"组卡片"是 history-detail 和 training-detail 都用的，可以抽成自定义组件 `components/set-card/` 减少重复 | |
| X-04 | 自定义动作 **媒体图片上传**：虽然 V3 才需要 mediaUrl，但文件存储策略可以先调研 |
| X-05 | **拼音首字母库**替换 helpers.ts 中手写 PinyinFirstLetterMap（pinyin-pro / pinyinjs 等，要评估包体） | |

---

## 4. 关键锚点速查（Agent 开发快速定位）

| 需求点 | 文档位置 | 代码位置 |
|--------|---------|----------|
| 动作字段英文枚举 → 中文 | [需求.md §4.3](file:///d:/WorkspaceDocs/IronTrace/需求.md#L153-L191) | [constants.ts](file:///d:/WorkspaceDocs/IronTrace/miniprogram/utils/constants.ts#L1-L88) |
| 内置动作数据 | [需求.md §9 附录](file:///d:/WorkspaceDocs/IronTrace/需求.md#L340-L388) | [exercises.json](file:///d:/WorkspaceDocs/IronTrace/miniprogram/data/exercises.json) |
| sets 内嵌结构示例 | [需求.md §4.1](file:///d:/WorkspaceDocs/IronTrace/需求.md#L68-L86) | [storage.ts saveSession](file:///d:/WorkspaceDocs/IronTrace/miniprogram/utils/storage.ts#L111-L128) |
| 训练容量计算（热身过滤） | [需求.md §6.1 + §5.1.2 边界](file:///d:/WorkspaceDocs/IronTrace/需求.md#L297) | [storage.ts calcSessionVolume](file:///d:/WorkspaceDocs/IronTrace/miniprogram/utils/storage.ts#L93-L99) |
| 1RM Epley 边界（reps=1） | [需求.md §6.2](file:///d:/WorkspaceDocs/IronTrace/需求.md#L301-L306) | [storage.ts calc1RM](file:///d:/WorkspaceDocs/IronTrace/miniprogram/utils/storage.ts#L219-L224) |
| 新训练自动结束上次 | [需求.md §5.1.2 边界](file:///d:/WorkspaceDocs/IronTrace/需求.md#L225) | [storage.ts createNewSession](file:///d:/WorkspaceDocs/IronTrace/miniprogram/utils/storage.ts#L157-L178) |
| 动作选回添加到训练 | `training-detail` → `exercise-list?mode=pick` 约定 | [exercise-list onPickExercise](file:///d:/WorkspaceDocs/IronTrace/miniprogram/pages/exercise-list/index.ts#L160-L183) |

---

## 5. 下一次开发建议起步顺序

如果后续直接续做，**按最高性价比排序**：

1. **V1-01 + V1-02**：先在微信开发者工具里把项目跑起来，这是一切调试的前提
2. **V1-03**：跑一次完整链路，把潜在结构问题（如 setNo 在多动作情况下是否冲突）提前发现 → 报告问题再修
3. **V1-04**：扩充拼音首字母表，因为动作库搜索是入口体验，否则立刻能感觉到"搜不到"
4. **V1-06**：加上 stepper 快捷 ±2.5/1.25 按钮，这是录入效率核心
5. 做完这 4 个，V1 就可以进入用户实际使用测试阶段了
