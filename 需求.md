# 微信小程序健身记录工具需求文档（V1-V3）

## 1. 项目概述

### 1.1 项目背景
开发一个微信小程序，名称为铁迹（IronTrace），用于个人健身记录。核心功能是记录每次训练的动作、重量、组数，并提供统计分析和训练建议。本需求文档聚焦于前三阶段（V1、V2、V3）的功能实现，暂不包含完整的训练计划模块和高级建议模块（V4）。

### 1.2 目标用户
- 主要用户：开发者本人（个人使用）。
- 可能扩展：其他健身爱好者。

### 1.3 核心目标
- 快速记录每次训练数据（动作、重量、组数、次数）。
- 自动填充上次训练重量，减少输入成本。
- 数据云端同步，防止丢失。
- 展示训练容量、1RM 趋势和 PR 记录。

---

## 2. 参考项目与产品

| 名称 | 类型 | 借鉴点 |
|------|------|--------|
| wger | 开源后端 | 动作库数据模型、训练日志结构 |
| 训记 | 商业小程序 | 动作库分类、组记录交互、容量图表 |
| Strong | 商业 App | 组卡片自动填充上次重量、计时器、历史列表 |
| Hevy | 商业 App | 训练容量曲线、PR 标识 |
| Fitbod | 商业 App | 训练建议逻辑（后续版本参考） |

---

## 3. 技术架构

### 3.1 技术选型
- **前端**：微信原生小程序 + **TypeScript**（类型安全，匹配复杂数据模型）
- **UI 组件**：**Vant Weapp**（步进器/表单组件适配健身录入，社区资源丰富）
- **图表**：**ECharts for 微信小程序**（支持双轴、标记点，满足 V3 统计图表需求）
- **后端/数据**：微信云开发
  - 云数据库：训练记录、动作库、用户信息
  - 云函数：统计计算、数据聚合
  - 云存储：动作图片（V3 及以后）
- **本地存储**：V1 阶段使用 `wx.setStorageSync` 存储，V3 迁移至云数据库

### 3.2 页面结构（TabBar）

| Tab | 页面 | 功能 |
|-----|------|------|
| 训练 | 训练首页 | 开始训练、选择动作、记录组数据 |
| 动作库 | 动作列表 | 浏览、搜索动作，查看详情 |
| 统计 | 统计页面 | 训练容量趋势、1RM 趋势、PR 列表 |
| 我的 | 个人中心 | 身体数据、设置、数据导出 |

> 注：V1 阶段仅包含“训练”和“动作库”两个 Tab。**历史记录入口**放在训练首页顶部（“上次训练”卡片 + “查看全部历史”按钮），V3 扩展 TabBar 时迁移。

---

## 4. 数据模型设计

### 4.1 本地存储（V1 暂用）
- `builtinExercises`：内置动作库（静态 JSON 文件，30~50 个，随版本发布）
- `customExercises`：用户自定义动作（Storage 存储）
- `exercises`：启动时合并内置 + 自定义，供全局使用（不单独持久化）
- `sessions`：训练会话数组（**sets 内嵌结构**，见下示例）

> 💡 存储结构决策：V1 采用 sets 内嵌在 session 中，读写简单、天然级联保序；
> V3 迁移云数据库时再拆分为 `workout_sessions` 和 `workout_sets` 两个集合。

```javascript
// sessions 存储结构示例（sets 内嵌，数组顺序 = 训练顺序）
[
  {
    _id: "session_001",
    startTime: "2026-08-20T18:30:00+08:00",
    endTime: "2026-08-20T19:40:00+08:00",
    durationMin: 70,
    bodyWeight: 75,
    totalVolume: 8750,
    note: "状态不错",
    isCompleted: true,
    sets: [
      { _id: "set_001", exerciseId: "ex_bench_press", setNo: 1, weight: 60, reps: 8, isWarmup: false, rpe: 8, restSec: 120 },
      { _id: "set_002", exerciseId: "ex_bench_press", setNo: 2, weight: 62.5, reps: 6, isWarmup: false, rpe: 9, restSec: 150 }
    ]
  }
]
```

### 4.2 云数据库集合（V3 启用）

```javascript
// users 用户信息
{
  _id: "openid",
  nickname: "",
  gender: "male" | "female",
  birthYear: 1995,
  heightCm: 178,
  trainingGoal: "strength" | "muscle" | "fat_loss"
}

// exercises 动作库
{
  _id: "ex_bench_press",         // 内置动作前缀 ex_，自定义动作前缀 custom_
  name: "杠铃卧推",
  bodyPart: "chest",              // 枚举值：见 4.3 字段中英文映射表
  equipment: "barbell",          // 枚举值：见 4.3 字段中英文映射表
  type: "compound",              // 枚举值：compound / isolation，见 4.3
  met: 5.0,                      // 代谢当量，V4 用；自定义动作可留空
  instructions: "动作说明",
  mediaUrl: "cloud://...",       // 可选图片，V3 及以后启用
  isCustom: false                // 是否用户自定义动作
}

// workout_sessions 训练会话
{
  _id: "session_xxx",
  userId: "openid",
  startTime: "2026-08-20T18:30:00+08:00",
  endTime: "2026-08-20T19:40:00+08:00",
  durationMin: 70,
  bodyWeight: 75,              // 训练时体重，用于容量计算
  totalVolume: 8750,           // 单位 kg，由组记录聚合
  note: "状态不错",
  isCompleted: true
}

// workout_sets 组记录
{
  _id: "set_xxx",
  sessionId: "session_xxx",
  exerciseId: "ex_bench_press",
  setNo: 1,                    // 第几组
  weight: 60,                  // kg
  reps: 8,
  rpe: 8,                      // 主观疲劳度 1-10（可选）
  restSec: 120,                // 组间休息秒数（可选）
  isWarmup: false              // 是否热身组
}

// body_metrics 身体数据（V3 可选）
{
  _id: "metric_xxx",
  userId: "openid",
  date: "2026-08-20",
  weight: 75.2,
  bodyFat: 18,
  waistCm: 82
}
```

---

### 4.3 字段中英文枚举映射表
> **统一规则：数据库/代码字段值一律使用英文枚举，用户界面显示映射为中文。**

#### bodyPart（训练部位）
| 英文值 | 中文显示 |
|--------|----------|
| `chest` | 胸部 |
| `back` | 背部 |
| `legs` | 腿部 |
| `shoulders` | 肩部 |
| `arms` | 手臂 |
| `core` | 核心 |
| `cardio` | 有氧 |
| `full_body` | 全身 |

#### equipment（器械类型）
| 英文值 | 中文显示 |
|--------|----------|
| `barbell` | 杠铃 |
| `dumbbell` | 哑铃 |
| `machine` | 器械 |
| `cable` | 绳索 |
| `bodyweight` | 自重 |
| `kettlebell` | 壶铃 |
| `other` | 其他 |

#### type（动作类型）
| 英文值 | 中文显示 |
|--------|----------|
| `compound` | 复合动作 |
| `isolation` | 孤立动作 |

#### trainingGoal（训练目标）
| 英文值 | 中文显示 |
|--------|----------|
| `strength` | 力量 |
| `muscle` | 增肌 |
| `fat_loss` | 减脂 |
| `general` | 综合 |

---

## 5. 功能需求（分阶段）

### 5.1 V1：基础记录功能

#### 5.1.1 动作库
- **内置动作**：预置 30～50 个常见力量训练动作，字段包括名称、部位、器械类型、动作类型、动作说明。
- **自定义动作（V1 支持）**：用户可新增自定义动作：
  - 入口：动作列表页右上角“+”按钮
  - 表单字段：名称（必填）、部位 picker（必填，枚举 bodyPart）、器械 picker（必填，枚举 equipment）、动作类型 picker（type）、说明 textarea（可选）
  - 自定义动作 `_id` 格式：`custom_${Date.now()}`，`isCustom: true`
  - V1 限制：**仅允许添加，不支持编辑/删除**（编辑删除功能 V2 实现）
- **列表展示**：内置动作与自定义动作混合展示，按部位分组或平铺列表，支持搜索（含拼音首字母匹配，如“wt”匹配“卧推”）。
- **动作详情**：显示动作名称、部位（中文）、器械（中文）、动作类型（中文）、说明。
- **数据来源**：静态 JSON（内置）+ Storage（自定义）合并使用，V3 迁移至云数据库。

#### 5.1.2 自由训练记录
- **开始训练**：点击“开始训练”创建新训练会话，记录开始时间，`isCompleted: false`。
- **选择动作**：从动作库选择动作添加到本次训练（可重复添加同一动作）。
- **记录组数据**：每个动作可添加多组，UI 提供 ±2.5kg / ±1.25kg 快捷按钮匹配常用片重，每组字段：
  - 重量（kg）：支持 0.5 小数步进，范围 0~1000
  - 次数：仅整数，范围 1~100
  - 是否热身组（可选，开关，默认 false）
  - RPE（可选，1~10 整数步进器，10=力竭，附提示文案“10 为完全力竭”）
  - 休息时间（可选，秒，V1 默认留空，V2 计时器启用后自动填充）
- **训练备注（V1 支持）**：训练过程中或完成后可在会话上添加备注（note 字段）。
- **完成训练**：点击“结束训练”，记录 endTime，自动计算 durationMin、totalVolume，`isCompleted: true`，存入 sessions。
- **本地存储**：使用 `wx.setStorageSync('sessions', sessionsArray)` 保存。
- **边界场景处理**：
  - 中途意外退出（杀进程/崩溃）：下次启动检测到 `isCompleted: false` 的 session，弹窗“继续上次训练 / 丢弃该记录”。
  - 忘记结束训练（durationMin > 240 分钟）：自动标记异常，详情页提示“时长异常，可手动编辑”。
  - 新训练开始时上次未结束：自动以当前时间结束上次 session（补全 endTime/durationMin/totalVolume），toast 提示“上次训练已自动保存”。
  - 热身组过滤：totalVolume、V3 的 1RM/PR 统计均仅统计 `isWarmup: false` 的正式组。

#### 5.1.3 历史记录
- **入口**：训练首页顶部展示“上次训练”快捷卡片（含时长/容量/日期）+ “查看全部历史”按钮。
- 全部历史列表按日期倒序排列，展示每次训练的时长、总容量、动作数量。
- 点击某次训练查看详情（动作分组展示、各组数据、总容量、时长、备注）。
- 支持删除某次训练记录（弹窗二次确认）；因 sets 内嵌结构，删除 session 天然级联，无孤儿数据。

#### 5.1.4 验收标准
- 能够顺利完成一次训练记录流程（开始→选动作→录组→结束→查看详情）。
- 训练数据保存在本地，重启小程序后仍可查看；异常退出后弹窗询问是否继续。
- 动作库至少包含 30 个内置动作，可正常浏览、搜索（含拼音首字母）。
- 自定义动作添加成功，与内置动作混合展示，重启后不丢失。

---

### 5.2 V2：体验优化

#### 5.2.1 自动填充上次重量
- 当用户选择某个动作后，自动查询该动作最近一次训练中每个正式组的重量和次数，填充到组输入框中。
- 用户可手动修改填充值。

#### 5.2.2 组间计时器
- 每组完成后自动开始倒计时。
- **默认休息时间：120 秒**，全局统一（V3 支持每个动作自定义默认休息时间）。
- 计时器可暂停、重置、跳过，可 +10s 快捷延长。
- 倒计时结束提醒：震动 + 系统提示音（`wx.vibrateLong` + 音频提示）。

#### 5.2.3 训练历史优化
- 历史列表展示每次训练的时长、总容量、动作数量。
- 训练详情页支持编辑组数据（修改重量、次数、删除组）。
- 支持对某次训练添加备注。

#### 5.2.4 验收标准
- 选择动作后，输入框自动填充上次数据，且可修改。
- 计时器正常工作，倒计时结束有提醒。
- 历史记录可编辑和删除，编辑后数据更新。

---

### 5.3 V3：统计与云同步

#### 5.3.1 接入微信云开发
- 开通云开发环境，创建云数据库集合（users, exercises, workout_sessions, workout_sets, body_metrics）。
- 用户首次登录时创建用户记录。
- 将本地存储的训练数据迁移至云数据库（提供一次性迁移工具或自动同步）。

#### 5.3.2 数据云同步
- 训练记录保存时同时写入云数据库，若网络失败则暂存本地，待网络恢复后同步。
- 云数据库作为主要数据源，本地存储仅作缓存。

#### 5.3.3 统计功能
- **训练容量趋势**：计算每次训练的总容量（Σ重量×次数×组数），以折线图或柱状图展示近 30 天/90 天趋势。
- **1RM 估算与趋势**：使用 Epley 公式估算每个动作的 1RM，展示其历史变化。
- **PR 记录**：自动识别每个动作的重量或容量个人纪录，在动作详情或统计页突出显示。
- **每周训练频率**：显示每周训练天数。

#### 5.3.4 数据导出
- 支持导出训练数据为 JSON 或 CSV 文件，便于备份。

#### 5.3.5 验收标准
- 数据能正常上传至云数据库，多端可查看。
- 统计图表能正确展示容量和 1RM 趋势。
- PR 记录准确识别并显示。

---

## 6. 核心算法

### 6.1 训练容量计算
```
训练容量 = Σ (重量 × 次数) 对所有正式组求和
```
单位：kg

### 6.2 1RM 估算（Epley 公式）
```
当 reps ≥ 2 时: 1RM = weight × (1 + reps / 30)
当 reps = 1 时: 1RM = weight （直接取重量，无需公式估算）
```
用于统计页面中的趋势图和 PR 判断；仅基于正式组（`isWarmup: false`）。

### 6.3 热量消耗估算（可选，V4 实现）
```
消耗热量(kcal) = MET × 体重(kg) × 时长(小时)
```
力量训练 MET 值：3.5 ~ 6.0，根据训练强度选择。

---

## 7. 开发里程碑

| 阶段 | 主要任务 | 预计产出 |
|------|----------|----------|
| V1 | 搭建 TS + Vant 项目框架，实现动作库（含自定义动作）、训练记录（含输入校验/备注/边界处理）、本地存储（内嵌结构）、历史查看 | 可用的记录小程序 |
| V2 | 增加自动填充、计时器、历史编辑 | 体验优化的版本 |
| V3 | 接入云开发，实现数据同步和统计图表 | 具有云同步和统计功能的版本 |

---

## 8. 注意事项

- 小程序若仅个人使用，可不发布，使用开发版/体验版即可。
- 动作库图片需注意版权，建议使用无版权素材或自己拍摄。
- 训练建议功能（V4）需避免医疗建议表述，仅提供训练参考。
- 云开发环境权限规则（V3 配置）：`users` / `workout_sessions` / `workout_sets` / `body_metrics` 仅创建者可读写；`exercises` 所有用户可读，仅管理员可写。
- **热身组过滤原则**：训练容量、1RM 估算、PR 识别均基于正式组（`isWarmup: false`）。
- **Epley 公式边界**：reps=1 时 1RM 直接取重量，避免公式放大。
- **重量输入 UI**：组数据录入页提供 ±2.5kg / ±1.25kg 快捷按钮，匹配健身房常用片重规格。

---

## 9. 附录：内置动作示例（部分）

```json
[
  {
    "_id": "ex_bench_press",
    "name": "杠铃卧推",
    "bodyPart": "chest",
    "equipment": "barbell",
    "type": "compound",
    "isCustom": false,
    "instructions": "平躺在卧推凳上，双脚踩实地面，握距略宽于肩，下放至胸部后推起至手臂伸直。"
  },
  {
    "_id": "ex_squat",
    "name": "杠铃深蹲",
    "bodyPart": "legs",
    "equipment": "barbell",
    "type": "compound",
    "isCustom": false,
    "instructions": "双脚与肩同宽，脚尖略外展，杠铃置于斜方肌上，屈髋屈膝下蹲至大腿平行地面后站起。"
  },
  {
    "_id": "ex_deadlift",
    "name": "硬拉",
    "bodyPart": "back",
    "equipment": "barbell",
    "type": "compound",
    "isCustom": false,
    "instructions": "杠铃贴近胫骨，屈髋屈膝握杠，背部挺直，伸髋伸膝拉起至身体直立后控制下放。"
  },
  {
    "_id": "ex_pullup",
    "name": "引体向上",
    "bodyPart": "back",
    "equipment": "bodyweight",
    "type": "compound",
    "isCustom": false,
    "instructions": "双手正握单杠，握距宽于肩，核心收紧，拉至下巴过杠后控制下放。"
  },
  {
    "_id": "ex_shoulder_press",
    "name": "哑铃推举",
    "bodyPart": "shoulders",
    "equipment": "dumbbell",
    "type": "compound",
    "isCustom": false,
    "instructions": "坐于训练凳上，背部贴紧靠垫，哑铃举至肩侧，推至头顶正上方后控制下放。"
  }
]
```

---

本文档作为 V1-V3 开发的需求依据，供开发 agent 参考执行。